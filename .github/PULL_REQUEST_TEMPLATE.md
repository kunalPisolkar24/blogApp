---
name: Pull Request
about: Changes targeting the dev branch
title: "Short imperative title, no prefix"
labels: ""
assignees: ""
---

## Summary

<!-- What this change does and why. 2-3 sentences. Reference the issue if
any (e.g. "Closes #12"). -->

## Test plan

<!-- The exact commands you ran, per service (see the service matrix in
AGENTS.md). Examples:

- [ ] `make test` + `make lint` (services/ai)
- [ ] `make test` + `make vet` + `make fmt` (services/content)
- [ ] `npm test` + `npm run lint` (services/user)
- [ ] `npm test` + `npm run lint` (frontend)
- [ ] Integration tests run (touches DB/Kafka/Redis/Qdrant)
-->

- [ ]
- [ ]

## Codegen

<!-- Check if generated code was regenerated (proto stubs, Prisma client,
GraphQL types). Not needed when no schema changed. -->

- [ ] Not applicable — no schema changes
- [ ] Regenerated (specify: proto / prisma / graphql)

## Notes

<!-- Anything reviewers should know: env vars, migrations, deployment
steps, follow-up work. -->